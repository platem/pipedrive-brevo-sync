- [x] **Pipedrive Service**: Created `$lib/server/services/pipedrive.service.ts` with `getFilters()` that fetches deal-type filters using `PIPEDRIVE_API_TOKEN` header. Defined `PipedriveFilter` interface matching API response.
- [x] **Server-Side Data & Actions**: Implemented `+page.server.ts` `load` function that upserts Pipedrive filters to local DB on every page load, marks deleted filters (both missing from Pipedrive and `active_flag: false`), and returns synced filter list with `brevoListId`. _(Note: `active_flag: false` from Pipedrive sets `deletedAt` timestamp for soft-delete. **Fix:** Replaced buggy `sql\`NOT IN (${ids.join(',')})\``with`notInArray()` to properly generate SQL with separate values)_
- [x] **Error Page**: Created `src/routes/(app)/+error.svelte` using SvelteKit's idiomatic error pattern. Replaced `redirect()` with `error()` in `+page.server.ts` for proper HTTP error handling. Displays Polish error message with retry button that navigates to `/`.
- [x] **Layout Logic & Logout**: Updated `(app)/+layout.svelte` with centered logo (always visible), back button (only on `/new` and `/overwrite` routes) using `ArrowLeft` from `@lucide/svelte`, and logout form. Created `/logout/+page.server.ts` with form action that invalidates session via `invalidateSession()`, deletes cookie via `deleteSessionTokenCookie()`, and redirects to `/login`.
- [x] **Dashboard Page Assembly**: Updated `+page.svelte` to receive filters data via auto-generated `PageData` type from `./$types`. UI already implements Mode Selection Card spec with centered container (`max-w-4xl`), two-column layout with vertical separator, and navigation buttons using `goto()` for `/new` and `/overwrite`.
